-- PART 2 of 3
-- GENERATED by tools/qm_paycert_rebuild.py — do not hand-edit.
-- Historical MPW payment certificates. Per work order the source is
-- whichever of (a) the كشف حساب جزئي columns or (b) the monthly
-- «جميع الشركات» sheets under دفعات الوزارة ties to the ministry
-- tracking report; copied-forward month sheets are excluded.
-- Work orders that tie to neither are left on their existing data and
-- listed in Desktop\quantities-backfill\paycert-rebuild-report.md.
-- Paste 0040 FIRST, then these parts IN ORDER. Re-runnable: the
-- backfill-sourced certificates are rebuilt, hand-made ones untouched.
do $qmpc$
declare
  v_contract bigint;
  v_c bigint;
  v_k bigint;
  v_item bigint;
begin
  select id into v_contract from qm_contracts where code = 'HAW9';
  if v_contract is null then raise exception 'run 0033 first'; end if;


  -- ══ دفعة 14 — حتى 2026-01-05 ══
  delete from qm_pay_cert_lines where cert_id in (select id from qm_pay_certs where contract_id = v_contract and cert_no = 14 and source = 'mpw');
  delete from qm_pay_certs where contract_id = v_contract and cert_no = 14 and source = 'mpw';
  insert into qm_pay_certs (contract_id, cert_no, period_end, source, status, note)
  values (v_contract, 14, '2026-01-05', 'mpw', 'certified', 'استيراد تاريخي — دفعات الوزارة') returning id into v_c;
  insert into qm_changelog (entity, entity_id, action, field, line_ref, old_value, new_value, actor_email)
  values ('paycert', v_c, 'create', '', '', '', 'استيراد دفعة الوزارة رقم 14', 'backfill');
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5644.0, 1636.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3560.0, 961.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 86 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/86 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 55.95)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 573.5, 1617.27)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 263.2, 11796.624)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 416.11, 18516.895)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5644.0, 30590.48)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3039.13, 7658.608)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 292.0, 2236.72)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 75.0, 1640.25)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 822.78)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1108.27, 4122.764)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 84.47, 356.463)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7588.9, 2200.781)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5549.16, 1498.273)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 16.78, 89.437)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 86 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/86 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 18.65)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 32.5, 91.65)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1937.04, 28571.34)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 206.45, 9253.089)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 204.77, 9112.265)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7589.0, 41132.38)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5175.05, 13041.126)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 320.5, 2455.03)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20.0, 246.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 72.78)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.0, 1371.3)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 30.0, 1252.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5539.0, 1606.31)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5558.6, 1778.752)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11097.6, 776.832)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 16 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ا';
  if v_item is null then raise exception 'bop 4/16ا missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5539.0, 14512.18)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 18 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/18ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5558.6, 19065.998)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8557.84, 26786.039)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 703.24, 4353.056)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7810.52, 2265.051)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12983.99, 4154.877)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20794.51, 1455.616)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 517.32, 1619.212)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1742.6, 10786.694)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5587.35, 1787.952)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5587.35, 391.115)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2366.59, 7407.427)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 27.46, 169.977)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2347.68, 7348.238)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 28;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.5, 15.51)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 28;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 116 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/116 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11.55, 47.702)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 28;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 97 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/97 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.5, 35.145)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 554.0, 160.66)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 554.0, 149.58)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 19.39, 869.06)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.85, 215.825)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 554.0, 3002.68)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 30.0, 75.6)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/24ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1470.82, 29063.403)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 882.0, 2848.86)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1323.0, 2553.39)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1206.91, 4019.01)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2530.0, 683.1)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4038.6, 1171.194)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4038.6, 1090.422)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 493.79, 2631.901)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 141.75, 6353.235)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 66.6, 2963.7)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3587.0, 19441.54)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 168.5, 544.255)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 54 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/54 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1137.67, 7155.944)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 72 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/72 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4801.4, 35242.276)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2001.0, 5042.52)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 46 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/46 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 322.5, 4837.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 72 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/72 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1075.0, 354.75)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1075.0, 75.25)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 11 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/11 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1075.0, 118.25)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 17 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/17ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1075.0, 3278.75)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 19 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'د';
  if v_item is null then raise exception 'bop 4/19د missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1075.0, 5493.25)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/24ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2485.6, 49115.456)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 31;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 18.0, 50.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 31;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 116 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/116 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 15.6, 64.428)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 31;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 18.0, 45.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 31;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 97 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/97 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 18.0, 115.02)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 57 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/57 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 30.0, 2219.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 59 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/59 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 30.0, 1917.9)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 89 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/89 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 42.0, 441.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 70 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/70 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 44.64, 24.998)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.02, 26.757)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 14.4, 40.608)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 111 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/111 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 21.0, 163.59)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 112 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/112 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 104.57, 429.783)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1493.91, 8186.627)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.46, 423.997)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 25.0, 63.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 96 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/96 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 135.7, 610.65)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 172 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/172 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 18.0, 321.3)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 176 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/176 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 20.06)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 179 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/179 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 19.0, 1355.27)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 233 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/233 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.95, 122.227)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 242 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/242 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 410.64)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 245 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/245 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 295.44)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 280 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/280 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.0, 607.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1501.38, 6335.824)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 327.36, 1535.318)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1761.31, 5865.162)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 440.0, 1240.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.68, 120.118)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 311.0, 3828.41)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 129.0, 2361.99)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 14.0, 306.18)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 308.4, 1147.248)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5660.0, 23885.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1043.1, 4892.139)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7192.07, 23949.593)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1891.0, 5332.62)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.16, 276.091)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 40.0, 306.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 968.0, 11916.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 853.0, 15618.43)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 60.0, 1312.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 799.65, 3374.523)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 775.82, 2583.481)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2137.6, 619.904)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2137.6, 577.152)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 268.75, 757.875)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 88.65, 3973.293)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 98.57, 4386.365)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2137.6, 11585.792)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 173.5, 334.855)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1600.0, 4032.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 200.4, 1535.064)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 68.35, 841.388)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 43.74)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 40;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 296 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/296 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4194.0, 3439.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 40;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 297 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/297 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 340.6, 279.292)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 40;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 298 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/298 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 43.1, 35.342)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 40;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 300 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/300 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 389.5, 327.18)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 40;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 302 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/302 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 124.8, 127.296)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 40;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 303 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/303 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 905.3, 923.406)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 131 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/131 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2202.7, 1211.485)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 133 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/133 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 598.6, 371.132)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 134 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/134 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 883.3, 574.145)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 135 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/135 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 201.5, 141.05)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 136 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/136 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 289.4, 214.156)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 138 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/138 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 181.8, 158.166)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 141 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/141 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 323.3, 375.028)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 156 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/156 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4680.6, 1965.852)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;



  -- ══ دفعة 15 — حتى 2026-02-05 ══
  delete from qm_pay_cert_lines where cert_id in (select id from qm_pay_certs where contract_id = v_contract and cert_no = 15 and source = 'mpw');
  delete from qm_pay_certs where contract_id = v_contract and cert_no = 15 and source = 'mpw';
  insert into qm_pay_certs (contract_id, cert_no, period_end, source, status, note)
  values (v_contract, 15, '2026-02-05', 'mpw', 'certified', 'استيراد تاريخي — دفعات الوزارة') returning id into v_c;
  insert into qm_changelog (entity, entity_id, action, field, line_ref, old_value, new_value, actor_email)
  values ('paycert', v_c, 'create', '', '', '', 'استيراد دفعة الوزارة رقم 15', 'backfill');
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 13711.1, 3976.219)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4218.21, 1349.827)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 17026.81, 1191.877)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 16 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ا';
  if v_item is null then raise exception 'bop 4/16ا missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 13711.1, 35923.082)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 18 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/18ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3315.71, 11372.885)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 27636.0, 8014.44)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 15869.0, 5078.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 43505.0, 3045.35)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6318.6, 39112.134)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 46 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/46 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 32.47, 487.05)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 14954.0, 4336.66)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12420.64, 3974.605)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 9 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/9 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6649.44, 3723.686)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 30374.64, 2126.225)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 11 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/11 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3324.72, 365.719)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 25.88, 381.73)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 345.56, 1081.603)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2043.5, 12649.265)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 98.81, 367.573)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 446.47, 1884.103)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 463.12, 1542.19)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1245.5, 361.195)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1245.5, 336.285)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 36.31, 193.532)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 57.34, 2569.979)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 3 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/3 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 14.28, 1276.061)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1245.5, 6750.61)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1969.98, 4964.35)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 180.6, 1383.396)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 43.74)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 822.78)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 222.12)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/24ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1636.36, 32334.474)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 108.0, 401.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 97.2, 323.676)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1448.0, 419.92)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1448.0, 390.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 48.0, 135.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 66.71, 2989.942)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 57.23, 2546.735)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 850.0, 4607.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3032.2, 7641.144)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 48.0, 367.68)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 31;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.7, 3.731)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 31;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.0, 16.92)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 31;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 115 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/115 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20.23, 65.748)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 31;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 116 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/116 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20.23, 83.55)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 31;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.7, 31.374)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 31;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 19.0, 61.37)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 31;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 97 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/97 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.0, 38.34)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 57 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/57 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 40.0, 2959.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 59 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/59 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 40.0, 2557.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 89 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/89 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 60.0, 630.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.5, 7.995)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 111 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/111 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 31.16)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 112 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/112 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 53.0, 217.83)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.15, 96.363)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 800.0, 2584.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.5, 8.685)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 901.8, 2272.536)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 96 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/96 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 66.1, 297.45)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 172 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/172 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 71.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 179 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/179 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 285.32)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 245 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/245 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 73.86)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 280 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/280 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 270.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 341.55, 1270.566)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3113.62, 13139.476)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 863.3, 4048.877)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2289.86, 7625.234)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2535.0, 735.15)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2535.0, 684.45)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 847.6, 2390.232)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 95 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/95 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 111.0, 1041.18)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 596.7, 8801.325)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 79.49, 3562.742)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 165.38, 7359.41)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2535.0, 13739.7)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 777.0, 1958.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 610.6, 7516.486)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 237.0, 4339.47)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 111.0, 4039.29)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 30.0, 656.1)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 40 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/40 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 146.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3144.74, 13270.803)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 306.9, 1439.361)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 548.93, 1827.937)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4787.0, 1388.23)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5549.7, 1498.419)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 326.0, 919.32)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 95 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/95 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 195.0, 1829.1)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 176.76, 7922.383)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 300.0, 13350.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4787.0, 25945.54)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4000.0, 10080.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 97.0, 1194.07)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 132.0, 2416.92)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 28 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/28 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 167.0, 4306.93)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 103.0, 4183.86)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 129.0, 7430.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8.0, 174.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 40 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/40 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8.0, 292.16)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 36;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 296 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/296 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3141.3, 2575.866)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 36;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 300 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/300 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 59.4, 49.896)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 36;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 302 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/302 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 217.9, 222.258)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1626.85, 471.786)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1626.85, 439.25)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 48.81, 2187.664)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 141.91, 6314.995)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1626.35, 8814.817)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2333.54, 5880.521)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 38;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 296 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/296 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8165.1, 6695.382)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 38;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 297 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/297 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 125.2, 102.664)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 38;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 298 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/298 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 32.0, 26.24)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 38;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 300 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/300 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 870.9, 731.556)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 38;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 302 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/302 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 96.1, 98.022)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 171.95, 648.251)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 940.25, 3497.73)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11100.0, 46842.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1400.0, 6566.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8000.0, 26640.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 787.2, 228.288)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 787.2, 212.544)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 400.0, 2132.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 83 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/83 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 39.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2339.3, 6596.826)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 95 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/95 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 257.0, 2410.66)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 208.03, 9323.905)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 97.84, 4353.88)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 787.2, 4266.624)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 230.0, 742.9)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 920.24, 2319.005)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 120.0, 919.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 374.0, 4603.94)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 612.0, 11205.72)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 28 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/28 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 180.0, 4642.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 250.0, 9097.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 183.0, 10075.98)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 95.0, 2077.65)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 40 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/40 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 18.0, 657.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 548.52)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 17 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 17/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 25.5, 116.79)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;



  -- ══ دفعة 16 — حتى 2026-03-05 ══
  delete from qm_pay_cert_lines where cert_id in (select id from qm_pay_certs where contract_id = v_contract and cert_no = 16 and source = 'mpw');
  delete from qm_pay_certs where contract_id = v_contract and cert_no = 16 and source = 'mpw';
  insert into qm_pay_certs (contract_id, cert_no, period_end, source, status, note)
  values (v_contract, 16, '2026-03-05', 'mpw', 'certified', 'استيراد تاريخي — دفعات الوزارة') returning id into v_c;
  insert into qm_changelog (entity, entity_id, action, field, line_ref, old_value, new_value, actor_email)
  values ('paycert', v_c, 'create', '', '', '', 'استيراد دفعة الوزارة رقم 16', 'backfill');
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 30.0, 159.9)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 63.3, 2816.85)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 56 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/56 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 200.0, 1316.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 21.0, 858.27)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 106.0, 4426.56)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/24ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 182.0, 3596.32)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 57 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/57 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12.0, 887.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 59 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/59 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 15.0, 958.95)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 34 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/34 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.62, 9.448)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 70 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/70 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 14.0, 7.84)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 0.54)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 30.0, 8.7)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 30.0, 8.1)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7.43, 39.602)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 22.2, 62.604)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 111 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/111 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 15.0, 116.85)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 112 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/112 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 70.25, 288.728)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 115 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/115 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 33.4, 108.55)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 117 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/117 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 32.4, 149.364)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.6, 116.532)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.0, 267.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 18.5, 100.27)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 70 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/70 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 55.3, 388.759)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 37.1, 71.603)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 24.0, 60.48)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 40.87)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 83.52)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 96 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/96 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 92.3, 415.35)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 97 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/97 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 22.2, 141.858)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 172 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/172 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 15.0, 267.75)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 179 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/179 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 15.0, 1069.95)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 136.88, 509.194)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2108.17, 8896.477)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 880.33, 2931.499)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1907.0, 553.03)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1907.0, 514.89)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 195.0, 549.9)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 150.5, 2219.875)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 83.69, 3750.986)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 100.73, 4482.485)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1907.0, 10335.94)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 831.5, 2095.38)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 61.0, 467.26)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 195.0, 3570.45)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 40 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/40 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.0, 219.12)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 274.26)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 41.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 17 and band = 9 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 17/9 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.5, 16.965)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 689.21, 2563.861)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2058.16, 9652.77)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 376.82, 1254.811)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3409.7, 988.813)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2647.9, 714.933)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 18.0, 95.94)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 95 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/95 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 37.0, 347.06)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5430.13, 80094.417)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 200.79, 8999.408)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3410.6, 18485.452)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 278.0, 2129.48)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 1097.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 304.8, 4495.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 79.5, 299.715)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 859.75, 3198.27)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1050.0, 4431.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 230.0, 1078.7)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 70 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/70 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 77.0, 43.12)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3593.0, 1041.97)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3593.0, 970.11)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1440.65, 7678.665)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 655.6, 1848.792)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 214.28, 9604.03)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 186.54, 8301.03)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3593.0, 19474.06)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3352.6, 8448.552)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1119.55, 8575.753)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 221.0, 2720.51)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 29.35, 537.399)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 125.0, 4548.75)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 43.0, 940.41)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 42 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/42 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 494.1)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 46 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/46 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.7, 261.036)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 1097.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.0, 375.84)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 17 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 17/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 133.0, 609.14)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2096.39, 670.845)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2096.39, 146.747)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 17 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/17ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2096.39, 6393.989)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 44;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 296 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/296 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7972.4, 6537.368)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 44;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 297 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/297 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.5, 5.33)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 44;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 298 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/298 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 428.0, 350.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 44;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 300 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/300 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 21.6, 18.144)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 44;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 302 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/302 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 250.6, 255.612)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 131 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/131 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2957.5, 1626.625)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 133 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/133 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1315.6, 815.672)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 135 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/135 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1301.1, 910.77)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 136 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/136 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 637.0, 471.38)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 138 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/138 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1572.9, 1368.423)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 142 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/142 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 764.6, 993.98)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 156 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/156 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8548.7, 3590.454)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;



  -- ══ دفعة 17 — حتى 2026-04-05 ══
  delete from qm_pay_cert_lines where cert_id in (select id from qm_pay_certs where contract_id = v_contract and cert_no = 17 and source = 'mpw');
  delete from qm_pay_certs where contract_id = v_contract and cert_no = 17 and source = 'mpw';
  insert into qm_pay_certs (contract_id, cert_no, period_end, source, status, note)
  values (v_contract, 17, '2026-04-05', 'mpw', 'certified', 'استيراد تاريخي — دفعات الوزارة') returning id into v_c;
  insert into qm_changelog (entity, entity_id, action, field, line_ref, old_value, new_value, actor_email)
  values ('paycert', v_c, 'create', '', '', '', 'استيراد دفعة الوزارة رقم 17', 'backfill');
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 75.0, 3065.25)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.0, 375.84)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1140.0, 330.6)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1140.0, 307.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 32.64, 1462.925)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 53.03, 2359.835)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1127.0, 6108.34)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 742.0, 1869.84)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 122.61)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 17.0, 709.92)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1197.8, 17667.55)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 33.0, 1348.71)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 35;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 17.0, 709.92)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 40.87)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 37;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 13.0, 542.88)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.0, 33.93)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 616.0, 2291.52)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 584.2, 2465.324)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1199.14, 3993.136)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 70 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/70 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 72.42, 40.555)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1002.0, 270.54)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 55.31, 294.802)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 248.15, 699.783)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1195.96, 17640.41)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 55.67, 2495.129)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 30.47, 1355.915)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1583.0, 8579.86)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1310.18, 3301.654)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 95.0, 727.7)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 45.0, 553.95)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 108.15, 1980.226)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.0, 131.22)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 42 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/42 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 741.15)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 46 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/46 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.5, 435.06)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 17 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 17/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 41.0, 187.78)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5143.53, 1645.93)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5143.53, 360.047)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 17 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/17ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5143.53, 15687.766)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 92.0, 287.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 132.0, 817.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1794.0, 520.26)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 263.0, 71.01)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 35.31, 188.202)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.84, 441.029)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11.8, 525.1)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1794.0, 9723.48)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2274.0, 5730.48)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 206.79, 769.259)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1412.68, 5961.51)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1410.32, 4696.366)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 475.8, 1341.756)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.6, 206.172)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.97, 132.165)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10.8, 82.728)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 141.0, 1735.71)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 195.0, 3570.45)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 28 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/28 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 52.0, 1341.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 77.0, 2802.03)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 24.0, 524.88)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 47;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 296 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/296 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7673.2, 6292.024)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 47;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 297 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/297 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 398.8, 327.016)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 47;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 298 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/298 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 671.1, 550.302)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 47;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 299 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/299 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 234.8, 192.536)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 47;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 300 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/300 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 67.0, 56.28)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 47;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 302 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/302 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 563.0, 574.26)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 131 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/131 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1698.5, 934.175)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 133 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/133 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1911.2, 1184.944)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 134 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/134 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 284.6, 184.99)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 135 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/135 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 418.9, 293.23)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 136 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/136 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 718.2, 531.468)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 137 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/137 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 248.5, 198.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 140 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/140 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 337.5, 354.375)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 142 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/142 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 138.5, 180.05)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 143 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/143 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 621.7, 932.55)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 156 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/156 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6377.6, 2678.592)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 130 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/130 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 115.5, 61.215)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 131 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/131 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4001.4, 2200.77)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 133 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/133 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2394.4, 1484.528)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 135 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/135 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1366.6, 956.62)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 136 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/136 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 803.3, 594.442)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 138 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/138 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 508.5, 442.395)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 140 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/140 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 171.3, 179.865)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 141 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/141 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 230.5, 267.38)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 142 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/142 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 228.3, 296.79)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 143 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/143 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 200.3, 300.45)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 145 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/145 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 150.0, 313.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 156 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/156 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10170.1, 4271.442)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 51;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 296 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/296 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7405.23, 6072.289)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 51;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 297 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/297 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 36.0, 29.52)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 51;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 298 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/298 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 779.6, 639.272)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 51;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 300 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/300 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 40.5, 34.02)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 51;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 302 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/302 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 496.5, 506.43)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 57 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/57 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 77.0, 5696.46)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 59 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/59 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 115.0, 7351.95)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 70 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/70 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.5, 5.32)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 0.27)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.97, 5.17)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8.6, 24.252)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 111 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/111 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20.0, 155.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 112 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/112 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 158.24, 650.366)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 117 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/117 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 40.96, 188.826)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.55, 248.751)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.3, 10.659)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 185.25, 466.83)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 12 and band = 17 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 12/17 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 750.0, 9247.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 96 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/96 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 214.4, 964.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 97 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/97 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.9, 63.261)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 172 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/172 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 17.0, 303.45)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 179 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/179 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 18.0, 1283.94)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 222 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/222 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 379.71)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 233 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/233 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 128.66)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 242 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/242 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 136.88)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 245 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/245 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 73.86)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 280 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/280 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7.0, 472.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;



  -- ══ دفعة 18 — حتى 2026-05-05 ══
  delete from qm_pay_cert_lines where cert_id in (select id from qm_pay_certs where contract_id = v_contract and cert_no = 18 and source = 'mpw');
  delete from qm_pay_certs where contract_id = v_contract and cert_no = 18 and source = 'mpw';
  insert into qm_pay_certs (contract_id, cert_no, period_end, source, status, note)
  values (v_contract, 18, '2026-05-05', 'mpw', 'certified', 'استيراد تاريخي — دفعات الوزارة') returning id into v_c;
  insert into qm_changelog (entity, entity_id, action, field, line_ref, old_value, new_value, actor_email)
  values ('paycert', v_c, 'create', '', '', '', 'استيراد دفعة الوزارة رقم 18', 'backfill');
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3778.4, 1095.736)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1830.0, 494.1)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2302.31, 33959.073)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 64.05, 2870.721)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 83.42, 3712.19)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3778.4, 20478.928)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2582.8, 6508.656)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 40.87)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11.25, 42.413)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 603.35, 2244.462)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1658.1, 6997.182)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 511.64, 2399.592)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 622.86, 2074.124)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 70 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/70 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.58, 0.325)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3452.6, 1001.254)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2450.6, 661.662)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 48.75, 259.837)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 193.95, 546.939)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6720.01, 99120.148)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 100.03, 4483.345)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 56.74, 2524.93)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 3 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/3 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 14.3, 1277.848)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1869.6, 10133.232)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1501.38, 3783.478)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 96.0, 735.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 97.95, 3564.401)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10.0, 218.7)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 40 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/40 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.0, 219.12)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 46 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/46 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.9, 87.012)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.0, 1645.56)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 296.16)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 32.0, 1307.84)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 38.0, 1586.88)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 27642.0, 8016.18)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4966.82, 1589.382)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 32608.82, 2282.617)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 16 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ا';
  if v_item is null then raise exception 'bop 4/16ا missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 27642.0, 72422.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 17 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/17ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3648.16, 11126.888)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 164.1, 2420.475)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4498.33, 14079.773)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9995.87, 61874.435)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 27.72)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 33 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ا';
  if v_item is null then raise exception 'bop 4/33ا missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 16.0, 559.84)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 100.0, 422.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 154.5, 514.485)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1884.0, 546.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 45.37, 241.822)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 57.5, 162.15)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.41, 18.376)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 40.02, 1780.89)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1884.0, 10211.28)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2665.78, 6717.766)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 28 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/28 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 50.0, 1289.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 354.2, 1317.624)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2658.24, 11217.773)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3056.93, 10179.577)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1874.5, 543.605)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1874.5, 506.115)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 764.5, 2155.89)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 95 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/95 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 168.0, 1575.84)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 469.41, 6923.798)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 129.5, 5804.19)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 65.73, 2924.985)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 3 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/3 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20.16, 1801.498)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1874.5, 10159.79)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1471.8, 3708.936)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 191.5, 1466.89)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 165.0, 2031.15)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 127.0, 2325.37)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 28 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/28 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 64.0, 1650.56)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 133.0, 4839.87)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 69.0, 3799.14)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 33 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/33 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 99.0, 8319.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 22.0, 481.14)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 40 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/40 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12.0, 438.24)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.0, 1645.56)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 131 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/131 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2160.9, 1188.495)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 133 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/133 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1236.4, 766.568)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 134 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/134 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 136.3, 88.595)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 135 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/135 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 335.0, 234.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 136 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/136 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 270.8, 200.392)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 138 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/138 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 62.3, 54.201)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 141 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/141 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 659.2, 764.672)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 156 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/156 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4981.0, 2092.02)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 164.27, 693.219)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 213.16, 999.72)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 327.8, 1091.574)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 677.0, 196.33)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 105.0, 28.35)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 84.1, 237.162)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 318.7, 4700.825)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.32, 417.722)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.72, 121.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 677.0, 3669.34)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1021.8, 2574.936)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.0, 68.94)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 84.1, 1539.871)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.0, 131.22)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 49;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4446.9, 1423.008)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 49;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4446.9, 311.283)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 49;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 19 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/19 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4446.9, 13696.452)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 57 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/57 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 32.0, 2367.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 59 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/59 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 32.0, 2045.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 203.67, 757.652)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 500.93, 2349.362)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 436.61, 1453.911)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 25.0, 7.25)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 25.0, 6.75)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11.66, 62.148)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.3, 9.306)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 95 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/95 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 54.0, 506.52)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 111 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/111 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10.0, 77.9)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 112 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/112 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 109.8, 451.278)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 23 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/23 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 36.8, 168.544)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 154.96, 849.181)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 44.74, 140.036)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 40.75, 1826.415)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11.2, 498.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 25.0, 135.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 19.1, 61.693)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 60.91, 153.493)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 114.1, 874.006)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 54.0, 3110.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 42 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/42 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 247.05)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7.0, 1919.82)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 167.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 261.36, 378.972)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 65 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/65 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1134.0, 879.984)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 96 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/96 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 64.5, 290.25)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 97 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/97 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 38.0, 242.82)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 172 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/172 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.0, 160.65)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 179 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/179 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.0, 641.97)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 280 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/280 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.0, 337.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 144.77, 538.544)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4711.98, 19884.556)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3110.73, 10358.731)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1414.8, 3989.736)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 28.58, 1280.956)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.34, 282.13)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 527.2, 4038.352)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 598.6, 10960.366)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 28 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/28 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 121.0, 3120.59)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 44.0, 962.28)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2405.15, 35475.963)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 168.0, 2068.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 209.77, 780.344)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1094.9, 4620.478)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 332.79, 1560.785)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1256.96, 4185.677)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 409.0, 1153.38)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 15.38, 684.41)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 21.0, 160.86)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 118.0, 1452.58)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 196.0, 3588.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 74.0, 2692.86)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 14.0, 306.18)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 17 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 17/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8.0, 36.64)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 56;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 2.398)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 56;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 119 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/119 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8.0, 339.464)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 56;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 127 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/127 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.8, 55.526)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 56;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 129 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/129 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 16.598)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 56;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 132 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/132 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8.0, 69.136)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 56;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 133 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/133 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20.0, 213.94)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 56;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 134 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/134 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.0, 24.45)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 56;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 137 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/137 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 95.47, 4191.706)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 56;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 145 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/145 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 166.83, 637.958)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;



  -- ══ دفعة 19 — حتى 2026-06-05 ══
  delete from qm_pay_cert_lines where cert_id in (select id from qm_pay_certs where contract_id = v_contract and cert_no = 19 and source = 'mpw');
  delete from qm_pay_certs where contract_id = v_contract and cert_no = 19 and source = 'mpw';
  insert into qm_pay_certs (contract_id, cert_no, period_end, source, status, note)
  values (v_contract, 19, '2026-06-05', 'mpw', 'certified', 'استيراد تاريخي — دفعات الوزارة') returning id into v_c;
  insert into qm_changelog (entity, entity_id, action, field, line_ref, old_value, new_value, actor_email)
  values ('paycert', v_c, 'create', '', '', '', 'استيراد دفعة الوزارة رقم 19', 'backfill');
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1050.0, 304.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1050.0, 283.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 36.76, 1647.583)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 19.85, 883.325)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1050.0, 5691.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 756.0, 1905.12)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 74.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 84.0, 3433.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 37.0, 1545.12)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 33;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 115 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/115 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7.5, 24.375)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 33;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.5, 13.86)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 33;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 297 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/297 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.5, 3.69)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1250.05, 5862.735)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1008.06, 3356.84)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 980.8, 284.432)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 547.3, 147.771)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 53.5, 150.87)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 95 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/95 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 107.85, 1011.633)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 365.73, 3379.345)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 19.16, 858.751)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 41.76, 1858.32)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.46, 584.049)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 980.8, 5315.936)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 757.52, 1908.95)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 53.5, 1946.865)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 107.85, 5938.221)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 40 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/40 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.0, 219.12)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 74.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 18.0, 735.66)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 34;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 17.0, 709.92)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 762.42, 221.102)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 762.42, 205.853)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 705.12, 10400.52)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 191.26, 1767.242)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 26.68, 1195.798)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 18.3, 814.35)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 762.42, 4132.316)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 466.75, 1176.21)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 28.0, 1144.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 40.0, 1670.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 15105.0, 4380.45)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 15105.0, 1057.35)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 16 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ا';
  if v_item is null then raise exception 'bop 4/16ا missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 15105.0, 39575.1)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1481.16, 4636.031)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4038.7, 24999.553)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 89.1, 823.284)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 594.0, 172.26)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 265.0, 71.55)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 18.27, 97.379)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.81, 439.684)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 15.2, 676.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 594.0, 3219.48)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 323.5, 624.355)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1308.5, 3297.42)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 79.65, 300.281)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 24.04, 89.429)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 792.0, 3714.48)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.07, 0.233)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5379.7, 1560.113)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4434.7, 1197.369)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 83 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/83 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 19.7)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 19.0, 53.58)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 147.68, 2178.28)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2478.44, 22900.786)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 121.76, 5457.283)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 155.32, 6911.74)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5379.7, 29157.974)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4563.25, 11499.39)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 19.0, 145.54)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 28 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/28 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 84.0, 2166.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 43.74)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 44.0, 1798.28)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 74.0, 3090.24)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 140 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/140 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 120.1, 126.105)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 20 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/20 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 50.0, 143.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1548.0, 448.92)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1450.0, 391.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 30.9, 1384.938)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 28.26, 1257.57)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1548.0, 8390.16)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 112.0, 361.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2684.75, 6765.57)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 49;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10526.9, 3052.801)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 49;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 14260.0, 4563.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 49;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 24786.9, 1735.083)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 49;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 15 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ا';
  if v_item is null then raise exception 'bop 4/15ا missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10526.9, 30949.086)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 49;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 17 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/17ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9420.0, 28731.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 49;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 19 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/19 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4840.0, 14907.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 15 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/15 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 595.6, 3013.736)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 21 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/21 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 502.73, 1367.426)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4780.0, 1386.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1795.0, 484.65)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 167.14, 890.856)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 66.0, 186.12)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 66.42, 2976.944)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 128.66, 5725.37)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4780.0, 25907.6)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1269.0, 2449.17)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4386.1, 11052.972)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 28 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/28 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 66.0, 1702.14)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 43.74)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 80 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/80 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4962.0, 72494.82)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 57.0, 16.53)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.48, 2.558)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 111 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/111 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 13.0, 101.27)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 112 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/112 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 54.36, 223.42)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 115 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/115 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 108.87, 353.827)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 116 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/116 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 63.93, 264.031)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 117 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/117 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 78.39, 361.378)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 19.5, 120.705)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10.75, 481.815)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 26.07, 1160.115)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 19 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/19 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.0, 40.86)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 359.4, 1947.948)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 281.3, 908.599)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 172.6, 434.952)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 21.87)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 40 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/40 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 146.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 62 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/62 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 521.97)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8.0, 326.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 16.0, 668.16)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 17 and band = 9 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 17/9 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 16.0, 41.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 96 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/96 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 64.77, 291.465)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 97 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/97 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 100.3, 640.917)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 172 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/172 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12.0, 214.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 176 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/176 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 80.24)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 179 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/179 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11.0, 784.63)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 222 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/222 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.0, 1898.55)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 233 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/233 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.06, 265.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 242 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/242 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.0, 684.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 243 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/243 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.0, 336.65)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 280 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/280 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 202.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 774.31, 3267.588)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 553.89, 1844.454)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1736.5, 503.585)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 43.0, 11.61)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 242.0, 682.44)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.62, 117.428)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 67.55, 3005.975)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1736.5, 9411.83)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1380.39, 3478.583)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 28 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/28 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 69.0, 1779.51)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11.0, 240.57)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 42 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/42 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 247.05)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 46 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/46 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.6, 154.688)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 62 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/62 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 173.99)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 203.4, 1879.416)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.7, 10.044)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.7, 20.682)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 173.0, 2129.63)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 274.26)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 16.2, 61.074)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 228.85, 851.322)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1953.66, 8244.445)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 652.62, 3060.788)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2045.64, 6811.981)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2627.48, 761.969)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1433.1, 386.937)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 612.55, 1727.391)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 95 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/95 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 134.0, 1256.92)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1682.06, 15542.234)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 49.53, 2219.935)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 71.09, 3163.505)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2627.48, 14240.942)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2236.51, 5636.005)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 227.05, 2794.986)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 321.65, 5889.411)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 28 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/28 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 63.85, 1646.691)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 134.0, 4876.26)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20.0, 437.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 40 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/40 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 146.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 57;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 296 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/296 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3890.5, 3190.21)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 57;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 297 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/297 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 380.3, 311.846)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 57;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 298 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/298 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 204.2, 167.444)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 57;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 300 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/300 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 600.8, 504.672)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 275.11, 1023.409)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 75.56, 318.863)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 55.34, 184.282)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5526.0, 1492.02)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3413.9, 990.031)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1597.7, 431.379)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.4, 26.508)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 198.6, 2929.35)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 667.75, 6170.01)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 116.14, 5205.395)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 49.97, 2223.665)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3413.9, 18503.338)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 54 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/54 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5526.0, 34758.54)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 814.87, 2053.472)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 253.0, 1937.98)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.4, 541.44)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 13.0, 284.31)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7.0, 1919.82)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 65;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 710.24, 2997.213)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 65;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 212.49, 707.592)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 65;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 184.0, 518.88)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 65;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.45, 64.525)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 65;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 128.0, 2343.68)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 65;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 28 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/28 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 56.0, 1444.24)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 65;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8.0, 174.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;



  -- ══ دفعة 20 — حتى 2026-07-05 ══
  delete from qm_pay_cert_lines where cert_id in (select id from qm_pay_certs where contract_id = v_contract and cert_no = 20 and source = 'mpw');
  delete from qm_pay_certs where contract_id = v_contract and cert_no = 20 and source = 'mpw';
  insert into qm_pay_certs (contract_id, cert_no, period_end, source, status, note)
  values (v_contract, 20, '2026-07-05', 'mpw', 'certified', 'استيراد تاريخي — دفعات الوزارة') returning id into v_c;
  insert into qm_changelog (entity, entity_id, action, field, line_ref, old_value, new_value, actor_email)
  values ('paycert', v_c, 'create', '', '', '', 'استيراد دفعة الوزارة رقم 20', 'backfill');
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 49.1, 182.652)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 340.2, 1435.644)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 283.75, 944.888)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 361.7, 104.893)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 14.7, 3.969)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 22.5, 119.925)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 168.36, 1555.646)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 16.17, 724.739)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20.3, 903.35)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 361.7, 1960.414)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 349.07, 879.656)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 36.1, 276.526)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 110.96, 1365.918)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11.0, 240.57)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 42 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/42 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 741.15)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 46 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/46 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.7, 454.396)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 41;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 822.78)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3168.0, 918.72)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3168.0, 221.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 42;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 16 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ا';
  if v_item is null then raise exception 'bop 4/16ا missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3168.0, 8300.16)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 116.23, 432.376)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 63.27, 210.689)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3830.0, 1110.7)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2728.0, 736.56)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 170.07, 906.473)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 130.4, 5844.528)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 153.66, 6837.87)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3830.0, 20758.6)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 948.5, 1830.605)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3751.72, 9454.334)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 43.5, 333.21)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 45;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 548.52)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 446.0, 129.34)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 148.0, 39.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 210.07, 1941.047)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.94, 266.231)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8.12, 361.34)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 446.0, 2417.32)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 495.0, 1247.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 17.0, 694.79)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 46;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 33.0, 1378.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 51.29, 190.799)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 155.5, 656.21)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 462.23, 2167.859)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 21 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/21 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 524.78, 1427.402)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 28.48, 94.838)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2203.0, 638.87)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 909.0, 245.43)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 93 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/93 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 46.7, 74.72)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 104.0, 293.28)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 47.86, 2145.085)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 50.09, 2229.005)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2203.0, 11940.26)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2044.5, 5152.14)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 67.7, 518.582)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 104.0, 1280.24)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.0, 131.22)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 48;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 274.26)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 147.59, 549.035)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 15 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/15 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 317.91, 1608.625)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 21 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/21 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 269.46, 732.931)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 78.9, 262.737)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2220.0, 643.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3360.0, 907.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 189.23, 1008.596)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 50.0, 141.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 103.58, 4642.456)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 71.34, 3174.63)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2220.0, 12032.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 993.0, 1916.49)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2113.9, 5327.028)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 107.5, 823.45)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 43.74)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 50;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 548.52)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 35 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/35 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 28.0, 1981.84)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.66, 36.418)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 168.26, 625.927)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 19.5, 82.29)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 44.64, 148.651)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 46 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/46 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 49.2, 738.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1160.55, 336.559)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.12, 0.64)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 18.0, 50.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 111 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/111 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11.0, 85.69)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 112 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/112 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 47.57, 195.513)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 23 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/23 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 925.0, 4236.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 30.0, 442.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 187.62, 587.251)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20.01, 896.848)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 37.73, 1678.985)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1194.55, 6474.461)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 151.3, 488.699)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1039.58, 2619.742)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 43.74)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 42 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/42 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 18.0, 4446.9)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 62 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/62 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 695.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8.0, 326.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 167.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 57.0, 72.219)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 21 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/21 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6517.0, 1264.298)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 43 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/43 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1100.0, 225.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 44 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/44 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1030.0, 282.22)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 64 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/64 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 227.0, 378.409)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 96 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/96 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 67.01, 301.545)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 172 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/172 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11.0, 196.35)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 176 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/176 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 80.24)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 52;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 179 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/179 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7.0, 499.31)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 53;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11337.92, 3628.134)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 53;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 7 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/7 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1545.5, 664.565)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 53;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12223.42, 855.639)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 53;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 17 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/17ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11337.92, 34580.656)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 53;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 19 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/19ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1545.5, 6321.095)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 53;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 28 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/28 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 866.97, 10134.879)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 53;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2420.99, 14985.928)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2466.2, 715.198)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 66.0, 17.82)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 15.35, 687.987)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 100.47, 4470.915)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2466.2, 13366.804)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1527.34, 3848.897)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 54;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 124.2, 1147.608)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 54.56, 202.963)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1978.9, 8350.958)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1481.71, 4934.094)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1913.02, 554.776)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 366.5, 98.955)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 86 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/86 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 18.65)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 654.8, 1846.536)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 427.13, 6300.167)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1166.04, 10774.21)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 28.29, 1267.958)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 66.11, 2941.895)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1887.9, 10232.418)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20.0, 64.6)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 57 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/57 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 30.0, 216.9)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1185.93, 2988.544)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 173.35, 1327.861)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 115.0, 1415.65)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 307.4, 5628.494)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 24.0, 524.88)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 55;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 274.26)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 513.17, 1908.992)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 247.54, 824.308)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1086.1, 314.969)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 86 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/86 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10.0, 186.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 33.93, 95.683)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 53.86, 2414.005)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 19 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/19 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1000.0, 4540.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 228.7, 1751.842)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 13.0, 284.31)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 42 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/42 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 19.1, 4718.655)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11.0, 3016.86)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 61;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 62 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/62 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 347.98)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 62;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 33.48, 124.546)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 62;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 580.34, 2449.035)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 62;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 137.66, 458.408)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 62;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 70.37, 375.072)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 62;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 199.4, 562.308)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 62;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20.55, 921.051)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 62;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.17, 96.565)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 62;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 94.5, 723.87)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 62;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 43.8, 539.178)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 62;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 61.1, 2223.429)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 62;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7.0, 153.09)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 63;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1195.33, 5044.293)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 63;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 647.0, 3034.43)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 63;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1541.0, 5131.53)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 63;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 212.0, 597.84)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 63;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 95 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/95 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 109.0, 1022.42)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 63;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7.9, 354.078)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 63;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.87, 261.215)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 63;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 106.0, 811.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 63;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 50.0, 615.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 63;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 56.0, 1025.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 63;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 36.0, 1462.32)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 63;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 73.0, 4019.38)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 63;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.0, 196.83)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 63;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 40 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/40 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 146.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 63;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 17 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 17/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10.0, 45.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 65;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 114.0, 3149.82)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 65;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 417.21, 1389.309)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 65;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 160.2, 43.254)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 65;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 95 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/95 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 114.0, 1069.32)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 65;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.77, 34.511)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 65;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 57 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/57 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 160.2, 1158.246)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 65;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 114.0, 4148.46)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 65;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 40 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/40 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 73.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 65;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 62 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/62 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 173.99)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 66;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 35 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/35 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 396.0, 28028.88)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 66;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 80 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/80 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7050.0, 103000.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 68;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 23074.28, 7383.77)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 68;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 23074.28, 1615.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 68;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 17 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/17ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 23074.28, 70376.554)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 68;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 193.6, 605.968)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 68;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 445.2, 2755.788)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 68;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 326.5, 3016.86)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 69;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 7 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/7 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 13415.69, 5768.747)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 69;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 13351.69, 934.618)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 69;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 19 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/19ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 13351.69, 54608.412)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 71;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5150.0, 1493.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 71;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1150.0, 310.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 71;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 40.0, 1792.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 71;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5150.0, 27913.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;

end $qmpc$;
