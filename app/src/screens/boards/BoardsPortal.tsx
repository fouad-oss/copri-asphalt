/* Placeholder — replaced by the boards module migration (wave 3). */
export default function BoardsPortal() {
  return <div style={{ padding: 24, fontFamily: "sans-serif" }}>boards module — migration in progress</div>
}
