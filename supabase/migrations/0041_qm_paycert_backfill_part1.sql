-- PART 1 of 3
-- GENERATED by tools/qm_paycert_rebuild.py — do not hand-edit.
-- Historical MPW payment certificates. Per work order the source is
-- whichever of (a) the كشف حساب جزئي columns or (b) the monthly
-- «جميع الشركات» sheets under دفعات الوزارة ties to the ministry
-- tracking report; copied-forward month sheets are excluded.
-- Work orders that tie to neither are left on their existing data and
-- listed in Desktop\quantities-backfill\paycert-rebuild-report.md.
-- Paste 0040 FIRST, then these parts IN ORDER. Re-runnable: the
-- backfill-sourced certificates are rebuilt, hand-made ones untouched.
do $qmpc$
declare
  v_contract bigint;
  v_c bigint;
  v_k bigint;
  v_item bigint;
begin
  select id into v_contract from qm_contracts where code = 'HAW9';
  if v_contract is null then raise exception 'run 0033 first'; end if;

  -- ══ دفعة 5 — حتى 2025-04-05 ══
  delete from qm_pay_cert_lines where cert_id in (select id from qm_pay_certs where contract_id = v_contract and cert_no = 5 and source = 'mpw');
  delete from qm_pay_certs where contract_id = v_contract and cert_no = 5 and source = 'mpw';
  insert into qm_pay_certs (contract_id, cert_no, period_end, source, status, note)
  values (v_contract, 5, '2025-04-05', 'mpw', 'certified', 'استيراد تاريخي — دفعات الوزارة') returning id into v_c;
  insert into qm_changelog (entity, entity_id, action, field, line_ref, old_value, new_value, actor_email)
  values ('paycert', v_c, 'create', '', '', '', 'استيراد دفعة الوزارة رقم 5', 'backfill');
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1131.0, 327.99)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1009.0, 272.43)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.12, 16.63)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 115 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/115 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 192.05, 624.163)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 17241.45, 5000.02)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 91.0, 4078.62)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 58.4, 2598.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3150.0, 17073.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1470.0, 3704.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 74.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 97 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/97 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 105.75, 675.742)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;



  -- ══ دفعة 6 — حتى 2025-05-05 ══
  delete from qm_pay_cert_lines where cert_id in (select id from qm_pay_certs where contract_id = v_contract and cert_no = 6 and source = 'mpw');
  delete from qm_pay_certs where contract_id = v_contract and cert_no = 6 and source = 'mpw';
  insert into qm_pay_certs (contract_id, cert_no, period_end, source, status, note)
  values (v_contract, 6, '2025-05-05', 'mpw', 'certified', 'استيراد تاريخي — دفعات الوزارة') returning id into v_c;
  insert into qm_changelog (entity, entity_id, action, field, line_ref, old_value, new_value, actor_email)
  values ('paycert', v_c, 'create', '', '', '', 'استيراد دفعة الوزارة رقم 6', 'backfill');
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 70 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/70 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7.1, 3.976)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 580.0, 168.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 280.0, 75.6)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 28.2, 150.306)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 115 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/115 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 27.63, 89.797)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12946.5, 3754.485)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 28.0, 1246.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 633.0, 1595.16)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 97 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/97 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 16.5, 105.435)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 280 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/280 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 67.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 280 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/280 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 270.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3816.4, 267.148)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 11 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/11 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3816.4, 419.804)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 18 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/18ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3816.4, 13090.252)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 20 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'د';
  if v_item is null then raise exception 'bop 4/20د missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3816.4, 23241.876)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3065.39, 9901.21)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2746.51, 5300.764)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2431.13, 6126.448)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 41.62, 154.826)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 18.06, 60.14)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 16.0, 4.32)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 678.0, 196.62)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 600.0, 162.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 36.3, 193.479)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 115 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/115 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 34.95, 113.588)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 117 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/117 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 52.47, 241.887)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 19.37, 868.163)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 35.05, 1559.725)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 678.0, 3674.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2457.26, 7936.95)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2563.42, 4947.401)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2373.33, 5980.792)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 17.0, 130.22)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 97 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/97 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 36.55, 233.554)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;



  -- ══ دفعة 7 — حتى 2025-06-05 ══
  delete from qm_pay_cert_lines where cert_id in (select id from qm_pay_certs where contract_id = v_contract and cert_no = 7 and source = 'mpw');
  delete from qm_pay_certs where contract_id = v_contract and cert_no = 7 and source = 'mpw';
  insert into qm_pay_certs (contract_id, cert_no, period_end, source, status, note)
  values (v_contract, 7, '2025-06-05', 'mpw', 'certified', 'استيراد تاريخي — دفعات الوزارة') returning id into v_c;
  insert into qm_changelog (entity, entity_id, action, field, line_ref, old_value, new_value, actor_email)
  values ('paycert', v_c, 'create', '', '', '', 'استيراد دفعة الوزارة رقم 7', 'backfill');
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3224.53, 13607.517)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3257.4, 10847.142)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 46 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/46 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 466.2, 6993.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3035.5, 880.295)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1068.0, 288.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 404.54, 2156.198)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 86 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/86 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 29.0, 540.85)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 852.4, 2403.768)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 16149.6, 4683.384)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6823.2, 2183.424)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6108.2, 427.574)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1723.41, 25420.298)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1200.0, 3756.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 700.0, 4333.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 149.03, 6679.525)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 42.04, 1870.78)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1615.5, 8756.01)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 334.8, 646.164)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4238.88, 10681.978)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 617.1, 4726.986)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 235.0, 2892.85)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 32.0, 699.84)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 29.0, 7953.54)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 280 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/280 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.0, 337.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4370.0, 305.9)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 11 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/11 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 693.33, 76.266)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 16 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ا';
  if v_item is null then raise exception 'bop 4/16ا missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4370.0, 11449.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 18 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/18ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 640.0, 2195.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 20 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'د';
  if v_item is null then raise exception 'bop 4/20د missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 693.33, 4222.38)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1635.74, 6084.953)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 685.54, 2892.979)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1657.17, 5518.376)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 500.0, 135.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2167.0, 628.43)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 911.0, 245.97)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 24.85, 132.451)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 86 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/86 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 24.0, 447.6)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 693.0, 1954.26)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 202.07, 9056.777)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 109.4, 4868.3)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2167.0, 11745.14)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 479.0, 924.47)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1568.0, 3951.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 385.0, 2949.1)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 308.0, 3791.48)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 29.0, 7953.54)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 375.16, 1395.595)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1375.39, 5804.146)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1439.15, 4792.37)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 43.45, 11.732)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 787.5, 228.375)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 657.0, 177.39)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 236.4, 1260.012)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 86 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/86 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 23.0, 428.95)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 428.0, 1206.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 107.76, 4829.803)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 787.5, 4268.25)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 232.2, 448.146)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 384.4, 968.688)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 148.2, 1135.212)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 220.8, 2718.048)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 37.0, 677.47)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 23.0, 6307.98)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 14;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 296 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/296 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2551.5, 2092.23)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 14;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 297 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/297 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 21.4, 17.548)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 131 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/131 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1990.5, 1094.775)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 133 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/133 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 915.0, 567.3)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 134 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/134 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 57.0, 37.05)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 135 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/135 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 260.0, 182.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 136 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/136 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 407.0, 301.18)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 138 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/138 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 452.0, 393.24)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 156 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/156 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4081.5, 1714.23)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;



  -- ══ دفعة 8 — حتى 2025-07-05 ══
  delete from qm_pay_cert_lines where cert_id in (select id from qm_pay_certs where contract_id = v_contract and cert_no = 8 and source = 'mpw');
  delete from qm_pay_certs where contract_id = v_contract and cert_no = 8 and source = 'mpw';
  insert into qm_pay_certs (contract_id, cert_no, period_end, source, status, note)
  values (v_contract, 8, '2025-07-05', 'mpw', 'certified', 'استيراد تاريخي — دفعات الوزارة') returning id into v_c;
  insert into qm_changelog (entity, entity_id, action, field, line_ref, old_value, new_value, actor_email)
0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;



  -- ══ دفعة 9 — حتى 2025-08-05 ══
  delete from qm_pay_cert_lines where cert_id in (select id from qm_pay_certs where contract_id = v_contract and cert_no = 9 and source = 'mpw');
  delete from qm_pay_certs where contract_id = v_contract and cert_no = 9 and source = 'mpw';
  insert into qm_pay_certs (contract_id, cert_no, period_end, source, status, note)
  values (v_contract, 9, '2025-08-05', 'mpw', 'certified', 'استيراد تاريخي — دفعات الوزارة') returning id into v_c;
  insert into qm_changelog (entity, entity_id, action, field, line_ref, old_value, new_value, actor_email)
  values ('paycert', v_c, 'create', '', '', '', 'استيراد دفعة الوزارة رقم 9', 'backfill');
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 600.68, 2234.53)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3349.0, 14132.78)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1351.02, 6336.284)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4457.0, 14841.81)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 46 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/46 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 561.87, 8428.05)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1208.1, 350.349)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 876.6, 236.682)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 300.0, 1599.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 86 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/86 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 31.0, 578.15)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1550.5, 4372.41)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 95 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/95 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 66.0, 619.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12600.8, 882.056)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5730.84, 84529.89)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1500.0, 4695.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1220.0, 7551.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 275.76, 12359.563)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1208.1, 6547.902)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 56 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/56 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 909.97, 5987.603)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 565.85, 1425.942)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 745.3, 5708.998)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 493.5, 6074.985)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 311.7, 5707.227)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 33 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/33 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 56.0, 4706.24)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 61.0, 1334.07)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 31.0, 8502.06)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 3 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/3 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4826.7, 3282.156)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5934.79, 4866.528)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 26.8, 101.036)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 125.9, 468.348)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 73.6, 310.592)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 134.7, 448.551)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1324.0, 3799.88)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 73 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/73 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2912.5, 1135.875)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 108.9, 29.403)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 81 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/81 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 32.19, 267.821)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8.7, 389.934)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 33.6, 2340.912)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 290.0, 1571.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 58 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/58 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 113.4, 746.172)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 72 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/72 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 372.8, 2736.352)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1732.0, 225.16)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 20 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/20 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 459.1, 1317.617)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 33.3, 3010.653)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8.0, 25.84)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 17 and band = 15 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 17/15 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 265.5, 833.67)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 590.08, 2195.098)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 535.56, 1783.415)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 947.0, 255.69)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2338.0, 678.02)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1016.0, 274.32)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 29.4, 156.702)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 203.0, 572.46)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 55.59, 2491.544)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 124.8, 5553.6)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2338.0, 12671.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 394.0, 760.42)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2816.0, 7096.32)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 103.0, 788.98)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 42.0, 517.02)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 58.0, 1061.98)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 552.92, 2056.862)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1056.2, 4457.164)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 93.0, 436.17)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1905.92, 6346.714)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 31.6, 8.532)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 700.0, 203.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 537.0, 144.99)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 300.0, 1599.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 86 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/86 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 15.0, 279.75)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 412.6, 1163.532)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 99.88, 4476.622)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 700.0, 3794.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 107.2, 206.896)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 295.9, 745.668)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 167.6, 1283.816)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 100.0, 1231.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 145.0, 2654.95)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 15.0, 4113.9)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 14;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 296 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/296 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 836.5, 685.93)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 14;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 302 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/302 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 151.6, 154.632)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 15;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 296 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/296 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12113.8, 9933.316)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 15;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 297 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/297 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 946.2, 775.884)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 15;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 298 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/298 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1240.3, 1017.046)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 15;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 300 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/300 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 695.0, 583.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 15;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 302 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/302 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 329.2, 335.784)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.17, 15.512)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1430.97, 6038.693)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 585.8, 1950.714)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 40.42, 10.913)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 799.8, 231.942)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 121.3, 32.751)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 93.08, 496.116)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 120.2, 338.964)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 400.0, 5900.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7.91, 354.526)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 75.96, 3380.22)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 799.8, 4334.916)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 56 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/56 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12.0, 78.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 166.31, 320.978)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 214.35, 540.162)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 70.2, 537.732)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 181.0, 6586.59)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 274.26)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 335.0, 90.45)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4284.0, 1242.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3251.0, 877.77)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20.54, 109.478)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 113.79, 5100.068)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 82.13, 3654.785)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4284.0, 23219.28)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1630.0, 5264.9)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 56 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/56 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 600.0, 3948.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1627.59, 3141.249)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2000.0, 5040.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 50.0, 2043.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 17;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 50.0, 2088.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 18;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 254.7, 68.769)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 18;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5013.5, 1453.915)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 18;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1844.7, 498.069)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 18;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 112.25, 598.293)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 18;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 64.56, 2893.579)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 18;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 64.05, 2850.225)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 18;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5013.5, 27173.17)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 18;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3788.0, 12235.24)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 18;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 56 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/56 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 900.0, 5922.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 18;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 256.1, 494.273)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 18;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2550.0, 6426.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 18;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 80.0, 3269.6)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 18;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 100.0, 4176.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 70 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/70 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 109.59, 61.37)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 111.8, 32.422)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.74, 3.944)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 111 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/111 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 24.0, 186.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 112 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/112 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 130.01, 534.341)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 115 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/115 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 300.0, 975.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 116 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/116 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 112.33, 463.923)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 117 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/117 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 117.34, 540.937)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 137 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/137 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 60.0, 193.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1907.0, 10450.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.6, 26.892)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 111.8, 361.114)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 135.7, 261.901)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 300.0, 756.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 158 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/158 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 357.0, 2555.406)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 96 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/96 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 135.65, 610.425)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 97 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/97 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 534.45, 3415.136)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 172 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/172 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 22.0, 392.7)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 175 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/175 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 14.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 176 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/176 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 20.06)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 179 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/179 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 24.0, 1711.92)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 222 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/222 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 16.0, 6075.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 233 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/233 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.48, 576.397)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 242 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/242 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 16.0, 2190.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 245 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/245 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 16.0, 1181.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 280 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/280 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7.0, 472.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6289.73, 19686.855)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;



  -- ══ دفعة 10 — حتى 2025-09-05 ══
  delete from qm_pay_cert_lines where cert_id in (select id from qm_pay_certs where contract_id = v_contract and cert_no = 10 and source = 'mpw');
  delete from qm_pay_certs where contract_id = v_contract and cert_no = 10 and source = 'mpw';
  insert into qm_pay_certs (contract_id, cert_no, period_end, source, status, note)
  values (v_contract, 10, '2025-09-05', 'mpw', 'certified', 'استيراد تاريخي — دفعات الوزارة') returning id into v_c;
  insert into qm_changelog (entity, entity_id, action, field, line_ref, old_value, new_value, actor_email)
  values ('paycert', v_c, 'create', '', '', '', 'استيراد دفعة الوزارة رقم 10', 'backfill');
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.23, 8.296)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8500.0, 35870.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7320.0, 24375.6)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 46 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/46 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2322.68, 34840.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2000.0, 540.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 720.0, 208.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 720.0, 194.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 81 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/81 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10.8, 89.856)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 86 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/86 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 28.0, 522.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 463.8, 1307.916)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 95 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/95 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 380.0, 3564.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 116 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/116 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 119.04, 491.635)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 19282.0, 5591.78)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3220.0, 1030.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 17424.0, 1219.68)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 133.0, 1961.75)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3700.0, 11581.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1700.0, 10523.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 251.24, 11260.577)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 520.0, 2818.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 56 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/56 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 414.1, 2724.778)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 280.0, 2144.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 95.5, 1175.605)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 18.31)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 180.0, 6550.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 150.0, 6093.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 150.0, 8640.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 33 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/33 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10.0, 840.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 37.0, 809.19)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 42 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/42 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 494.1)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 46 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/46 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 290.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 26.0, 7130.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 148.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 3 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/3 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1476.0, 1003.68)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 7;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 

  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 115 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/115 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 457.9, 1488.175)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11.18, 501.088)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.0, 29.07)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 153.73, 387.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 50.0, 383.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 96 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/96 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 130.0, 585.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 97 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/97 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 89.3, 570.627)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 172 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/172 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20.0, 357.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 175 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/175 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 14.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 179 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/179 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 21.0, 1497.93)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 222 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/222 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 379.71)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 233 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/233 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.07, 9.006)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 242 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/242 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 136.88)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 245 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/245 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 73.86)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 280 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/280 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 135.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10331.03, 32336.124)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 778.1, 4816.439)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 22;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 296 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/296 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8454.9, 6933.018)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 22;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 297 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/297 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 85.5, 70.11)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 22;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 298 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/298 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 66.9, 54.858)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 22;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 300 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/300 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 577.4, 485.016)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 22;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 302 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/302 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 448.0, 456.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 212.0, 57.24)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3393.0, 983.97)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2463.2, 665.064)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 86.21, 3863.932)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 47.45, 2111.525)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3393.0, 18390.06)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3000.0, 9690.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 56 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/56 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 361.0, 2375.38)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 111.19, 214.597)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2000.0, 5040.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9508.0, 2757.32)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9508.0, 665.56)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2000.0, 6260.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 500.0, 3095.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 15487.0, 4491.23)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12162.0, 851.34)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2500.0, 7825.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1000.0, 6190.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 74 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/74 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 52.5, 16.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 742.0, 200.34)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 261.0, 75.69)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 261.0, 70.47)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.25, 235.305)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 175.0, 948.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 56 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/56 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1602.0, 10541.16)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1731.0, 4362.12)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 21 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/21 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 23473.0, 4553.762)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 214.0, 422.65)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 328.0, 569.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 32 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/32 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 108.0, 374.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 34 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/34 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 17.0, 81.311)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 36 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/36 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.0, 88.569)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 43 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/43 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3962.0, 812.21)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 44 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/44 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2254.0, 617.596)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 61 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/61 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 290.0, 165.59)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 27;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 125 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/125 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2850.0, 6270.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 27;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 128 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/128 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1719.2, 5071.64)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 27;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 130 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/130 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 27000.0, 14310.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 27;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 131 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/131 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20000.0, 11000.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 27;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 133 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/133 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 50.0, 31.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 27;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 136 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/136 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 66.0, 48.84)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;



  -- ══ دفعة 11 — حتى 2025-10-05 ══
  delete from qm_pay_cert_lines where cert_id in (select id from qm_pay_certs where contract_id = v_contract and cert_no = 11 and source = 'mpw');
  delete from qm_pay_certs where contract_id = v_contract and cert_no = 11 and source = 'mpw';
  insert into qm_pay_certs (contract_id, cert_no, period_end, source, status, note)
  values (v_contract, 11, '2025-10-05', 'mpw', 'certified', 'استيراد تاريخي — دفعات الوزارة') returning id into v_c;
  insert into qm_changelog (entity, entity_id, action, field, line_ref, old_value, new_value, actor_email)
  values ('paycert', v_c, 'create', '', '', '', 'استيراد دفعة الوزارة رقم 11', 'backfill');
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2895.44, 10771.037)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 36.93, 155.845)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4000.0, 18760.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 15 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/15 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 72.0, 364.32)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6770.33, 22545.199)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 46 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/46 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 452.58, 6788.7)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1233.58, 333.067)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 851.7, 246.993)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 791.7, 213.759)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3000.0, 15990.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 482.6, 1360.932)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 95 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/95 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 238.9, 2240.882)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2161.9, 626.951)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4343.4, 1389.888)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8876.0, 621.32)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2821.55, 41617.863)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4099.3, 12830.809)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2922.0, 18087.18)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 78.9, 3536.298)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.61, 71.645)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 873.6, 4734.912)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 56 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/56 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item,

  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 15016.1, 47000.393)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 19;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 859.43, 5319.872)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 120.0, 446.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 70 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/70 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 143.34, 80.27)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.03, 10.82)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 86 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/86 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 55.95)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 34.0, 95.88)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 102 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/102 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 48.0, 66.72)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 111 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/111 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 23.0, 179.17)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 112 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/112 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 109.92, 451.771)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 116 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/116 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 37.95, 156.734)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 132.58, 726.538)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20.7, 927.774)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 19 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/19 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.67, 21.202)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 14.5, 78.59)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 9.69)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 56 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/56 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 153.0, 1006.74)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.6, 6.948)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 21.42, 53.978)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.5, 34.47)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 822.78)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 31.54, 54.722)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 65 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/65 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 40.0, 31.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 81 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/81 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20.31, 91.354)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 96 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/96 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 136.75, 615.375)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 97 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/97 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.2, 58.788)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 172 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/172 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 21.0, 374.85)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 179 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/179 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 22.0, 1569.26)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 222 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/222 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 759.42)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 233 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/233 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.87, 111.934)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 242 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/242 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 273.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 245 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/245 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 147.72)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 280 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/280 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7.0, 472.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12148.0, 3522.92)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12148.0, 850.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 16 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ا';
  if v_item is null then raise exception 'bop 4/16ا missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12148.0, 31827.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2631.15, 8235.499)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 745.25, 4613.097)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 32.5, 120.9)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 262.0, 70.74)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1633.0, 473.57)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1633.0, 440.91)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 72.93, 388.717)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 60.2, 2698.164)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 173.6, 7725.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1518.0, 8227.56)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1000.0, 3230.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 56 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/56 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 222.0, 1460.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6816.6, 17177.832)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10.0, 76.6)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 274.26)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 131 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/131 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3362.1, 1849.155)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 133 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/133 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1342.7, 832.474)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 135 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/135 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 849.0, 594.3)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 136 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/136 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1235.8, 914.492)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 138 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/138 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 879.1, 764.817)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 141 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/141 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 183.8, 213.208)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 23;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 156 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/156 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5800.0, 2436.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 5 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1223.0, 391.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 238.0, 16.66)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3648.0, 11418.24)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1538.4, 9522.696)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 196.32, 730.31)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 20 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/20 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 129.0, 370.23)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 71 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/71 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 100.0, 62.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 65000.0, 18850.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 38000.0, 2660.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.0, 224.1)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 25.0, 63.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 23 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/23 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 66.0, 370.26)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 38.0, 291.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.0, 1371.3)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 62 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/62 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8.0, 1391.92)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1506.54, 6357.599)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1408.52, 4690.372)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 169.59, 903.915)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 297.5, 838.95)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 27.49, 1232.102)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 206.5, 1581.79)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 42.0, 517.02)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 49.0, 897.19)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10.0, 218.7)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;



  -- ══ دفعة 12 — حتى 2025-11-05 ══
  delete from qm_pay_cert_lines where cert_id in (select id from qm_pay_certs where contract_id = v_contract and cert_no = 12 and source = 'mpw');
  delete from qm_pay_certs where contract_id = v_contract and cert_no = 12 and source = 'mpw';
  insert into qm_pay_certs (contract_id, cert_no, period_end, source, status, note)
  values (v_contract, 12, '2025-11-05', 'mpw', 'certified', 'استيراد تاريخي — دفعات الوزارة') returning id into v_c;
  insert into qm_changelog (entity, entity_id, action, field, line_ref, old_value, new_value, actor_email)
  values ('paycert', v_c, 'create', '', '', '', 'استيراد دفعة الوزارة رقم 12', 'backfill');
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1924.98, 9028.156)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 15 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/15 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 880.32, 4454.419)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.33, 1.099)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 46 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/46 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 93.68, 1405.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 100.14, 533.746)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 155.0, 437.1)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 95 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/95 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 20.1, 188.538)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 6;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 
and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/5 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1223.0, 391.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9795.88, 30661.104)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2600.0, 16094.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 71 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/71 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 37.1, 23.002)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11362.67, 3295.174)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 38362.67, 2685.387)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1662.86, 14367.11)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6413.48, 20074.192)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 99.16, 4444.351)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 62 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/62 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 695.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.0, 367.83)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 103 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/103 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 11.0, 459.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 135 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/135 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.0, 6.3)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 893.11, 3322.369)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4847.57, 20456.745)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7923.9, 26386.587)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 619.9, 179.771)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 560.0, 151.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 949.47, 5060.675)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1663.8, 4691.916)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 335.54, 15038.903)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 619.9, 3359.858)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 54.0, 104.22)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 321.0, 808.92)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1142.8, 8753.848)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 169.0, 3094.39)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 352.0, 12809.28)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 88.0, 1924.56)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 40 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/40 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 14.0, 511.28)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 16.8, 63.336)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2842.7, 13332.263)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2000.35, 7441.302)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7008.3, 29575.026)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8102.39, 26980.959)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 70 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/70 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 48.0, 26.88)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 72 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/72 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3259.1, 1075.503)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 285.0, 82.65)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 285.0, 76.95)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 996.45, 5311.079)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1434.5, 4045.29)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 152.38, 6829.672)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 9.13, 406.285)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 285.0, 1544.7)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 667.0, 1680.84)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 685.5, 5250.93)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 346.0, 4259.26)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 207.0, 3790.17)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 28 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/28 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 108.0, 2785.32)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 58.0, 1268.46)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10.0, 408.7)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;



  -- ══ دفعة 13 — حتى 2025-12-05 ══
  delete from qm_pay_cert_lines where cert_id in (select id from qm_pay_certs where contract_id = v_contract and cert_no = 13 and source = 'mpw');
  delete from qm_pay_certs where contract_id = v_contract and cert_no = 13 and source = 'mpw';
  insert into qm_pay_certs (contract_id, cert_no, period_end, source, status, note)
  values (v_contract, 13, '2025-12-05', 'mpw', 'certified', 'استيراد تاريخي — دفعات الوزارة') returning id into v_c;
  insert into qm_changelog (entity, entity_id, action, field, line_ref, old_value, new_value, actor_email)
  values ('paycert', v_c, 'create', '', '', '', 'استيراد دفعة الوزارة رقم 13', 'backfill');
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2225.0, 600.75)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 134.75, 718.217)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.5, 66.75)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 141.0, 764.22)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 9;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1324.87, 3338.672)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 30.47, 1365.665)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 10;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10.0, 408.7)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 752.31, 2798.593)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 392.53, 1656.477)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1882.17, 508.186)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 272.15, 1450.559)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 638.0, 1799.16)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 412.5, 6084.375)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 117.63, 5272.177)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 216.74, 9644.93)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 38.0, 73.34)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 256.85, 647.262)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 356.0, 4382.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 27 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/27 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 65.0, 1190.15)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 78.0, 2838.42)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 822.78)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 16;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 28.0, 1144.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 19;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 994.41, 3112.503)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 57 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/57 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 50.0, 3699.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 59 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/59 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 50.0, 3196.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 89 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/89 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 150.0, 1575.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 111 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/111 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 70.0, 545.3)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 116 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/116 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1006.4, 4156.432)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12.42, 556.664)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 19.98, 889.11)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 19 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/19 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 45.2, 205.208)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 41 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/41 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 376.2, 1215.126)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 355.0, 685.15)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 465.0, 1171.8)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 250.0, 1915.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 74 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/74 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.0, 566.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 21 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/21 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 28149.0, 5460.906)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 61 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/61 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2320.0, 1324.72)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 65 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/65 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 102.0, 79.152)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 68 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/68 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 32.0, 144.672)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 20;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 17 and band = 28 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 17/28 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 227.7, 2955.546)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 35824.05, 10388.975)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 35824.05, 2507.684)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 16 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ا';
  if v_item is null then raise exception 'bop 4/16ا missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 35824.05, 93859.011)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8983.08, 28117.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 21;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2348.76, 14538.824)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 4 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/4 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 25857.18, 7498.582)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 25857.18, 1810.003)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 24;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7314.8, 22895.324)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12686.52, 888.056)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1631.64, 5107.033)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 25;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 212.04, 1312.528)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 58.0, 15.66)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2176.78, 6813.321)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2398.92, 14849.315)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 34.0, 184.28)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 56 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/56 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 252.0, 1658.16)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 1097.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 62 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/62 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.0, 869.95)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 100 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/100 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12.0, 490.44)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 21 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/21 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4160.0, 807.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 7 and band = 65 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 7/65 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 750.0, 582.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 17 and band = 28 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 17/28 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 798.5, 10364.53)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 26;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 17 and band = 30 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 17/30 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 175.1, 2819.11)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 658.28, 2448.802)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1001.49, 4226.288)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2789.34, 9288.502)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1947.0, 564.63)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1925.0, 519.75)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 616.99, 3288.557)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 647.0, 1824.54)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 224.06, 10042.369)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1947.0, 10552.74)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 87 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/87 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 96.0, 185.28)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 953.41, 2402.593)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 425.0, 3255.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 114.0, 4148.46)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 40 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/40 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4.0, 146.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 64.8, 244.296)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1368.9, 6420.141)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 366.43, 16306.135)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 28 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/28 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 108.0, 2785.32)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 19.0, 415.53)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2.0, 548.52)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 29;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/24ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1358.17, 26837.439)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 12 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/12 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2196.06, 8169.343)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 13 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/13 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 2792.4, 11783.928)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10904.51, 36312.018)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 70 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/70 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 65.0, 36.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 72 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/72 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1288.45, 425.189)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 850.0, 246.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 850.0, 229.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1071.56, 5711.415)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 86 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/86 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 13.0, 242.45)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 94 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/94 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1202.5, 3391.05)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 534.8, 23969.736)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 193.2, 8597.4)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 850.0, 4607.0)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 698.0, 1758.96)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/24 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 710.5, 5442.43)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 26 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/26 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 25.0, 307.75)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 28 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/28 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 152.0, 3920.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 29 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/29 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 109.0, 3966.51)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 31 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/31 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 206.0, 11342.36)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 39 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/39 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 30.0, 656.1)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 52 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/52 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 8.0, 2194.08)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 10 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/10 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 10.0, 37.7)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 15 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/15 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5037.2, 25488.232)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 83 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/83 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 19.7)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 95 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/95 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 394.0, 3695.72)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 3 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/3 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 23.9, 2135.704)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 33 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/33 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 394.0, 33111.76)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 40 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/40 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 14.0, 511.28)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 97 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/97 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 5.0, 14.85)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 14 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/14 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1933.9, 9069.991)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 6 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 6/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 74.04)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 30;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 24 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = 'ب';
  if v_item is null then raise exception 'bop 4/24ب missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 4099.08, 80997.821)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 57 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/57 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 18.0, 1331.64)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 59 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/59 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 18.0, 1150.74)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 1 and band = 89 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 1/89 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 27.0, 283.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 70 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/70 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.7, 0.392)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 76 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/76 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 25.14, 6.788)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 77 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/77 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 17.5, 5.075)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 78 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/78 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 17.5, 4.725)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 79 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/79 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.05, 5.597)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 111 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/111 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 14.0, 109.06)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 112 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/112 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 92.53, 380.298)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 115 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/115 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 114.18, 371.085)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 2 and band = 116 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 2/116 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 7.78, 32.131)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 4 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 4/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 51.95, 284.686)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 1 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/1 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.16, 51.991)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 2 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/2 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 6.28, 279.46)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 25 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/25 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 17.5, 94.85)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 5 and band = 88 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 5/88 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 73.2, 184.464)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 96 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/96 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 128.25, 577.125)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 97 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/97 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 63.8, 407.682)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 172 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/172 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 12.0, 214.2)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 179 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/179 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 14.0, 998.62)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 222 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/222 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 1139.13)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 233 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/233 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 0.3, 38.598)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 242 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/242 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 410.64)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 245 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/245 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 3.0, 221.58)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;
  select id into v_k from qm_kashefs where contract_id = v_contract and kashef_no = 32;
  select id into v_item from qm_bop_items where contract_id = v_contract and bab = 22 and band = 280 and translate(coalesce(suffix,''), 'أإآ', 'ااا') = '';
  if v_item is null then raise exception 'bop 22/280 missing'; end if;
  insert into qm_pay_cert_lines (cert_id, kashef_id, bop_item_id, qty, amount)
  values (v_c, v_k, v_item, 1.0, 67.5)
  on conflict (cert_id, kashef_id, bop_item_id) do update set qty = qm_pay_cert_lines.qty + excluded.qty, amount = qm_pay_cert_lines.amount + excluded.amount;

end $qmpc$;
