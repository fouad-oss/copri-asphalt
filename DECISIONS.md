# DECISIONS.md — assumptions made during unattended runs

One line each: what was assumed and why. Newest at the bottom.

## 2026-07-13 — v2 brief adoption run (slices 0–5 continuation)

- The attached `CLAUDE_CODE_BRIEF_commitment_pipeline-v2.md` supersedes the committed `CLAUDE_CODE_BRIEF_commitment_pipeline.md`; both stay in the repo, CLAUDE.md points at the -v2 file — history preserved, no file deleted.
- `copri-frontend-SKILL.md` (Tailwind + shadcn + react-i18next stack) conflicts with the repo convention (single-file vanilla-JS `index.html`, no build step). Per the brief's preamble, memory files win on conventions: control slices continue in the existing single-file convention; the skill governs the separate visual-refresh track and its principles (patterns, bilingual rules, one-accent-action) are applied within the current stack where they don't require a build system. FLAGGED as the brief requires.
- `COPRI_dedup_review.xlsx` added to .gitignore (real vendor names = company books, never enter version control); it stays at the repo root because the accountant is actively filling it.
- Brief says capture is raw with NO PO reference; 0018 had added an optional LPO+line picker to the receiver form. Rework (0020): picker removed from field capture, mapping moved to the accountant daily batch with auto-suggestions; the 0018 no-PO flag machinery is kept as the exception path.
- Approval chains (0020): implemented as `approval_chain_gates` config rows (chain key, gate order, required capability). Seeded chain per brief: accountant-raised requests → finance_approver (Jimmy). Default chain for all other requests = single head-office approver gate (matches v1 behaviour). Adding PM→Jimmy→Admin later = config rows only.
- New pipeline capabilities as boolean columns (finance_approver, management) following the 0017 accountant/admin pattern rather than a roles table — smallest diff on the existing model; `plant manager` = requester whose pipeline_user_centers row is 5205 (no new column needed).
- Item master (0020): new `items` + `item_spectronova_ids` tables mirroring the vendors pattern; `commitment_lines.item_id` and `material_receipts.item_id` added NULLABLE alongside the existing free-text columns — hard FK enforcement deferred until the items master is seeded (dedup ITEMS sheet pending accountant decisions), so nothing breaks at paste time.
- Subcontract register (0021): `subcontracts` extends a CON commitment 1:1 (like blanket_lpos extends LPO) rather than a parallel register — keeps no-orphan rule and numbering intact.
- Materials-issued-to-sub ledger (0021): existing `material_receipts.subcontractor` free-text rows are the capture source; back-charge rows link receipts to subcontracts at accounting time (same map-then-approve pattern as PO matching).
- Blanket lines (0022): existing ceiling-based `blanket_lpos` gains `blanket_lines`; drawdown switches to qty-per-line for line-based blankets while legacy money-ceiling blankets keep working until re-registered (staged: `control_mode` column 'ceiling'|'lines').
- MFA (TOTP) for admin/finance-approver: Supabase Auth supports TOTP enrollment but the portal is plain-REST; enforcement is a pre-launch checklist item in SECURITY.md, not built this run.
- Dedup tool: `apply` consumes the existing root `COPRI_dedup_review.xlsx` format (the accountant already fills it) — the spec's `output\dedup-review-vendors.xlsx` name applies to fresh `propose` output in the same format.
- Slice 4 closed as config-complete: routing rules are `approval_rules` rows (0013) with enforcement hooks + inline WHY flags (0022); value-threshold auto-routing stays dormant until finance supplies the threshold (null = matches nothing, by design); restricting manual PO entry to exceptions deferred per the brief's own instruction ("do not force a switch before the register has earned trust").
- Line call-offs cannot carry a rate variance at request time (value derived server-side at the line's agreed rate); variance re-enters at GRN/invoice match where the daily batch shows amounts vs line rates — no separate variance table added yet.
- Blanket seeds: vendor rows for KNPC / Jawharat Berlin / testing inserted (0022); the blankets themselves are registered through the portal once finance supplies agreed rates and quantities (open inputs) — no placeholder ceilings invented.

## 2026-07-14 — front-end rebuild track (user-selected)

- User chose the full rebuild track: new app at `app/` (Vite + React + TS + Tailwind v4 + shadcn/ui radix-nova with RTL + TanStack Table + react-i18next + supabase-js). Legacy `index.html` stays live and untouched; screens migrate in waves.
- Brand hue: the copri.com logo PNG is a white knockout (unsampleable), so per the skill's brochure fallback the hue was sampled from `copri-brochure.pdf` vector fills — navy `#0C2E6D` (structure) + green `#00833E` (accents). `--brand`/`--primary` = **#00833E** (AA on white ≈4.9:1, replaces the #D35400 placeholder — the logo/brochure wins); navy = `--brand-ink` secondary. Ink-tinted logo generated (`app/src/assets/brand/copri-logo-ink.png`) since the white original is invisible on Paper.
- v1 DB stores Arabic status literals, not coded values — mapped to i18n keys at display time (`statusKey()`); recoding the DB waits for a dedicated migration.
- Wave 1 screens: login (logo + tagline), role-routed home ("waiting on you" tiles), accountant daily batch (QUEUE — map-then-approve with suggestions), commitment register (REGISTER — filter bar + twin received/invoiced bars), three-way match detail (DETAIL — metric strip + line states + linked docs). Request queue and remaining inventory migrate in later waves; the stub screen points at the legacy portal.
- Fonts self-hosted via @fontsource (Inter variable, IBM Plex Sans Arabic 400/600, JetBrains Mono) — no CDN, offline-friendly.
- Wave 2 (same night): entire pipeline portal migrated — Approvals/Commitments/Deliveries/Masters/Reports/Admin sections; field capture, clerk dispatch, milling, dashboards and the plant/finance desks deliberately stay on the legacy app until their own phases (offline capture, print flow).
- Wave 3 (same night, 4 parallel agents): dispatch (print TEMPLATE_HTML ported byte-identical; QR carries /dispatch/note/:id path instead of ?note= query), offline-first capture (localStorage queue, photo-first idempotent sync), milling (all portals + public report), boards (5 dashboards + plant/finance desks; SVG trend replaced by bar lists per the skill). The ENTIRE legacy app now has a new-look counterpart; the ?wo portal tile and engineer my-records home were the only omissions (their targets migrate with the WO portal later). Switch-over still gated on: paste 0020-0022, plant-printer test for the print flow, Vercel build wiring.
- Per-unit request lines (0023, user addition): every RF request may carry item lines (canonical items-master picker + free-text fallback, qty, unit, rate) — value derived like blankets; final approval materializes them as commitment_lines so matching works on RF-born POs. Lump-sum requests (and the legacy portal) keep working; the React form omits p_lines when empty so it still resolves pre-paste.

## 2026-07-14 — accounting pivot (BRIEF-accounting-pivot.md)

- Two Fouad decisions at kickoff: build in legacy `index.html` (React `app/` shelved from the deploy, kept for later re-use) and EVOLVE pipeline v2 — commitments/commitment_lines are the PO register, vendors/items masters take the SN seeds, bundles snap onto commitment_lines; export batches (0016) retire in favor of published bundles. The brief's "Apps Script + Sheets" stack line is stale boilerplate — everything is Supabase.
- Step-1 shelving amendment (Fouad): plant-manager and finance portals stay live; milling, ?wo, exec/project/acct boards shelved. "Desk selection view (Fouad only)" = the ?dash picker; its PIN gate waits for the auth phase.
- Note recon statuses (0024) stored as the brief's EN tokens (`matched`/`dispatched_not_received`/`received_not_dispatched`/`qty_mismatch`), unlike the v1 Arabic literals — the brief names them explicitly; UI translates.
- Asphalt recon semantics (0024): rejected receipt → `qty_mismatch` (a rejected note must never read matched; divergence is surfaced, not blocking); accepted receipt with no arrival weight = `matched` (engineer confirmed without weighing); equality is exact, no tolerance band until the accountant asks for one.
- Materials recon semantics (0024): capture-only channel — `matched` = mapped to a PO line AND approved in the 0020 daily batch; `dispatched_not_received`/`qty_mismatch` unused for materials.
- Bundle lifecycle guard (0024) allows verified→draft demotion (pre-publish correction); published is immutable except imported_flag/sn_reference/imported_at. Bundle content (lines) editable in DRAFT only.
- One-note-bills-once enforced by partial unique index on bundle_lines(note_source, note_ref) excluding adjusting bundles (corrections re-reference the original notes, negative qty allowed).
- bundle_lines snapshot delivery_date/note_no/site at bundling time so published bundles stay byte-stable regardless of later note edits.
- Supplier 5205 mapping seeded onto the existing internal vendor 'كوبري — مصنع الأسفلت' + a `plant_dispatch_supplier` settings row as the single client-readable config point.
- Canonization (0025) resolves ids SERVER-SIDE: BEFORE-INSERT triggers norm-match raw text to items/vendors, so canonical links exist regardless of client version — the frontend only swaps dropdown sources (canonical masters when ref_payload v3 carries them, legacy CONFIG lists otherwise; deploy order can't break capture either way).
- Asphalt canonization = mix→item mapping (`asphalt_mix_item`): Type III matched before II before I (substring containment); non-Type mixes stay unmapped. Trigger fires on insert + `update of mix` only, so receipt-status patches don't touch it.
- material_catalog / suppliers / subcontractors ref tables demoted to pre-0025 fallback; staff edit the items/vendors masters going forward (README+CLAUDE.md note).
- Unit on the capture form now follows the selected ITEM (canonical per-item UOM), falling back to the category unit on the legacy path.
- Approvals page (0026 + UI) lives as the «🧮 التسوية والتجميع» tab inside the ?rf portal (accountant/admin): channel switch أسفلت|مواد + views (step 4: rectification queue + PO line balances; step 5 adds bundling + transcription summary). The 0018 daily batch stays — it IS the mechanics that drive material captures to `matched`; the queue links to it.
- Asphalt rectification queue filters to Copri loads only: external-client dispatches are sales, never receive receipts and never bill against our POs — they'd otherwise flood dispatched_not_received forever.
- Channel split for PO balances = vendor mapped to SN 5205 (asphalt) vs everything else (materials); imported plant POs already resolve to the internal plant vendor, so the mapping covers them.
- Live DB reality: the 426 imported SN POs carry HEADERS ONLY (commitment_lines empty) — added po_lines_add RPC (append-only, active POs, accountant/requester) + inline line editor on every PO card in the balances view; the register's lines are typed by the accountant per the brief.
- commitments.sn_po added for SN's own PO number (imports backfilled from source_ref; manual entries get a form field with the PO/\d{3,5} soft WARN — never blocks); po_entry regrown with p_sn_po/p_po_date (old overload dropped, 0022 precedent).
- Bundling UI (0027): billing qty for an asphalt note = received weight, falling back to dispatched for accepted-unweighed notes; amount = qty × line rate rounded to 3 dp (rate-less lines bundle at 0 and show it — surfaced, not blocked).
- Adjusting bundles: created FROM a published bundle's card, listing its notes with qty-override inputs (negative = deduction, zero skipped); may target any PO line (a wrong-line publish is fixed by a negating adjustment on the wrong line + an adjusting addition on the right one, since the note-bills-once index blocks normal rebundling). Empty bundles refuse to publish.
- bundle_transcription view = the SN transcription layout AND the future data-page payload (step 7 filters published + token): Supplier uses the vendor's sn_name when mapped; PO Number = sn_po fallback our number; Item Code via item_spectronova_ids on the LINE's item.
- Last-used-line suggestion = `bundle_last_line` view (latest bundle per canonical item); applied only when every selected note shares one item and the accountant hasn't already picked a line.
- GRN generator (step 6) is pure frontend — document numbers are DERIVED from the source (GRN-DN-<note> per note, GRN-<bundle_no> per bundle), so a reprint is byte-identical with no counter, no table, no DB write. Distinct from the pipeline `grns` table (office-channel records); the brief's "our own numbering" satisfied without a new series, and nothing resembles SN's Stock_Receipt/xxxx.
- Per-note GRNs = A5 landscape (delivery-note template language: field boxes, sig boxes, kiosk print-color-adjust); per-bundle = A4 portrait with the notes table + totals. Signature blocks: مهندس الموقع (prefilled from the receipt's engineer / capture's receiver) + المحاسب (blank). Every sheet footers the "internal supporting document — not an SN document" disclaimer. QR omitted (brief: optional).
- GRN note list = received goods only (dispatch notes with a received leg or matched; all material captures); filters per brief = day / site / PO (PO resolved through bundle membership — unbundled notes print with an empty PO box).
