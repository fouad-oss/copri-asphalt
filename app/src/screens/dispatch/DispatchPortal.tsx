/* Placeholder — replaced by the dispatch module migration (wave 3). */
export default function DispatchPortal() {
  return <div style={{ padding: 24, fontFamily: "sans-serif" }}>dispatch module — migration in progress</div>
}
