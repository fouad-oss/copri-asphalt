/* Placeholder — replaced by the milling module migration (wave 3). */
export default function MillingPortal() {
  return <div style={{ padding: 24, fontFamily: "sans-serif" }}>milling module — migration in progress</div>
}
