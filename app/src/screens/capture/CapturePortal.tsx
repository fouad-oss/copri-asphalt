/* Placeholder — replaced by the capture module migration (wave 3). */
export default function CapturePortal() {
  return <div style={{ padding: 24, fontFamily: "sans-serif" }}>capture module — migration in progress</div>
}
